# Stage 0: UPC probe, GET /me/albums pagination, GET /me/playlists + /items for
# keeper detection, normalize into data/albums.json via the deterministic writer.
# See PLAN.md "Stage 0 — Poller" for the exact field list and the release_year /
# keeper split (year is not a stored field — see "Vet notes").
