# Stage 5: reconcile YYYY release_year playlists against albums.json, scoped to
# relocating already-known keepers only — never adds a non-keeper, never removes
# a keeper. Ship dry-run first. See PLAN.md "Stage 5 — Playlist reconciler".
